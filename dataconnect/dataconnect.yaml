specVersion: "v1beta"
serviceId: "ignitionshift"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ignitionshift-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
